import { ClearDialogDirective } from './clear-dialog.directive';

describe('ClearDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearDialogDirective();
    expect(directive).toBeTruthy();
  });
});
