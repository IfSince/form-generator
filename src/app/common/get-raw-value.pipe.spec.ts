import { GetRawValuePipe } from './get-raw-value.pipe';

describe('GetRawValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetRawValuePipe();
    expect(pipe).toBeTruthy();
  });
});
